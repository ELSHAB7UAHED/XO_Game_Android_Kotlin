<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Regular weight using system font -->
    <font
        android:fontStyle="normal"
        android:fontWeight="400"
        android:font="@android:font/roboto_regular" />
    <!-- Medium weight using system font -->
    <font
        android:fontStyle="normal"
        android:fontWeight="500"
        android:font="@android:font/roboto_medium" />
    <!-- Bold weight using system font -->
    <font
        android:fontStyle="normal"
        android:fontWeight="700"
        android:font="@android:font/roboto_bold" />
</font-family>